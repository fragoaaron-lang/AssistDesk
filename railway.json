{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "watchPatterns": [
      "server/**",
      "client/**"
    ]
  },
  "deploy": {
    "startCommand": "npm install --prefix server && npm start --prefix server"
  }
}
